// Obtain handles and environment properties

// Draw window.frames using localStorage.zoomLevel when draw is called


draw.draw = function = (){
	var width = window.innerWidth;
	var height = window.innerHeight;
	var zoomLevel = parseFloat(localStorage.zoomLevel);
	
}